import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ministerioApk',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
